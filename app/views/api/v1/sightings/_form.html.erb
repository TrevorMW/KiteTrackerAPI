<%= form_for(@api_v1_sighting) do |f| %>
  <% if @api_v1_sighting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_v1_sighting.errors.count, "error") %> prohibited this api_v1_sighting from being saved:</h2>

      <ul>
      <% @api_v1_sighting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
