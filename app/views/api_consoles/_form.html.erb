<%= form_for(@api_console) do |f| %>
  <% if @api_console.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_console.errors.count, "error") %> prohibited this api_console from being saved:</h2>

      <ul>
      <% @api_console.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
